通过问答方式讲解 Pairing Engine 的 Runtime 思维：Current Run 是 Processing Unit，Engine 根据 Current Run 更新 Context（Stack、Match），Context 是状态而不是主角。Pairing Engine 本质是状态机：Input → State Machine → New State。流程图已经可以直接翻译成代码：for loop、Stack Empty、Push、Can Close、Pop、Create Match、Return。建议统一决策命名为 'Stack Top 在等待什么'，便于扩展 Link、Image、HTML 等类型。分析复杂函数时固定按五步：Processing Unit、State、Questions、Actions、Control Flow。不要继续优化流程图，而是翻译成代码并通过测试验证 Runtime。